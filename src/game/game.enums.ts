export enum eGameState {
    PLAYING = 'Playing',
    VICTORY = 'Victory',
    DRAW    = 'Draw',
}

export enum eGameVersus {
    COMPUTED    = 'Computed',
    HUMAN       = 'Human',
}

export enum ePlayerSymbol {
    X = 'X',
    O = 'O',
}
